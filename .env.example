# Convex URLs come from `npx convex dev` (.env.local) and .env.production.
# Multiplayer now runs on Convex. Guests stay offline; signed-in players join
# the village through the same Convex deployment as saves.
